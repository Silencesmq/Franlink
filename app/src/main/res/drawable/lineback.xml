<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- 手指触摸时的button -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <!-- 填充的颜色 -->
            <solid android:color="#fdfdfd" />
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners android:radius="5dip" />
            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding
                android:left="10dp"
                android:top="10dp"
                android:right="10dp"
                android:bottom="10dp"
                />
            <stroke android:width="2dip" android:color="#ffffff" />
            <gradient
                android:startColor="#d9dddf"
                android:endColor="#d3d5d7"
                android:angle="270" />
        </shape>
    </item>

    <!-- 默认的（触摸之前）button -->
    <item>
        <shape android:shape="rectangle">
            <!-- 填充的颜色 -->
            <!--<solid android:color="#0088ff" />-->
            <!-- 设置按钮的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners android:radius="5dip" />
            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding
                android:left="10dp"
                android:top="10dp"
                android:right="10dp"
                android:bottom="10dp"
                />
            <stroke android:width="1dip" android:color="#5000" />
            <gradient
            android:startColor="#51A2B7"
            android:endColor="#B6EEEF"
            android:angle="270" />

        </shape>
    </item>
</selector>